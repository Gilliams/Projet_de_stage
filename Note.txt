Commande :

	java graph_embedding/Main Alkane output3.txt 2

Tout mettre dans ce putain de meme dossier, le src/graph... ne marche pas va savoir pk..

	javac src/graph_embedding/Main.java

Transforme le .java en class utilisable avec java

Commande pour mettre a jour le main.java 

	javac -sourcepath C:\Users\Florian\workspace\Stage\src src/graph_embedding/Main.java
(Ne pas oublier de se mettre a la racine, ici, Stage) 

___________________________________
Php :

isset 									= 
	Détermine si une variable est définie et est différente de NULL
strpos 									= 
	Cherche la position de la première occurrence dans une chaîne
	(1er arg = la chaine,
	 2e arg = la chaine a comparé,
	 3e arg = a partir d'ou, si valeur negatif commence a la fin)
filter_var_array 						=
	Récupère plusieurs variables et les filtre
FILTER_SANITIZE_STRING 					=
	Supprime les balises, et supprime ou encode les caractères spéciaux.
INPUT_GET/INPUT_POST 					= 
	Filtre les variables get et post en input
array_merge 							= 
	array_merge(tab1, tab2....)
	Fusionne plusieurs tableaux en un seul, a la suite, si chaine alors elle est remplacée si un int alors il est ajouté 
file_exists 							= 
	Vérifie si un fichier ou un dossier existe
array_walk/array_values 				= 
	Exécute une fonction fournie par l'utilisateur sur chacun des éléments d'un tableau
SERVER[REQUEST_METHOD] 					=
	Méthode de requête utilisée pour accéder à la page; i.e. 'GET', 'HEAD', 'POST', 'PUT'.
file_get_contents/file_put_contents 	=  
	Lit tout un fichier dans une chaîne, 5 param (
		1er : nom du fichier
		2e : use_include_path
		3e : context
		4e : offset(a partir d'ou)
		5e : maxlen(jusqu'ou)
		)
	Écrit un contenu dans un fichier, 4 param (
		1er : nom
		2e : data a ecrire
		3e : flag
		4e : context
	)

























































graph matching

node signature
matrice hongroise
apparaiement


+rapide
+precis

SVM = proba 
Hongroise = cherche chemin
